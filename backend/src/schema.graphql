type Query {
  items(limit: Int): [Item!]
}

enum ItemType {
  job
  story
  comment
  poll
  pollopt
}

type Item {
  by: User
  type: ItemType
  id: Int!
  url: String
  text: String
  title: String
  descendants: Int!
  kids: [Item!]
}

type User {
  id: String!
  karma: Int!
}
